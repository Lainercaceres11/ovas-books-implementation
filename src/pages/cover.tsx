import { CoverTitle } from '@layouts';

const Cover = () => {
  return (
    <CoverTitle
      title="Construcción de un modelo de aprendizaje para la integración exitosa en la UNAD"
      audio={{ title: 'assets/audios/audio.mp3', a11y: 'assets/audios/audio.mp3' }}
    />
  );
};

export default Cover;
